# SPA fallback - redirect all non-API routes to index.html
# Note: sitemap.xml is handled by Netlify function (see netlify.toml)
# robots.txt should be served automatically as a static file
/* /index.html 200

